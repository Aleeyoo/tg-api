# Telegram 服务器地址（可选）
TELEGRAM_HOST=t.me

# 静态资源代理前缀（可选）
STATIC_PROXY=/static/

# 白名单频道（可选，逗号分隔，匹配的走 KV 缓存）
# CHANNELS=your_channel,channel2

# 严格模式（可选，开启后只允许白名单频道）
# STRICT_MODE=true

# KV 缓存秒数（可选）
# CACHE_TTL=300
